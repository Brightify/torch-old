language: java
jdk: oraclejdk7
before_install:
- sudo apt-get update -qq
- sudo apt-get install -qq libstdc++6:i386 lib32z1 expect
- curl -L https://raw.github.com/embarkmobile/android-sdk-installer/version-2/android-sdk-installer
  | bash /dev/stdin --install=android-21 && source ~/.android-sdk-installer/env
script: mvn install -B
notifications:
  slack: brightify:G9XT4KDOPRLdFiVic9XYkUKQ
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/30f3e4b6091257a2e588
    on_success: change
    on_failure: always
    on_start: false